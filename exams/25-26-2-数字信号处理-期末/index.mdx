---
时间: 2025-2026学年第二学期
科目: 数字信号处理
阶段: 期末
类型: 本科
---

## 一

1. $x(n)=e^{-an}u(n) \quad (0<e^{-a}<1)$ 的离散时间傅里叶变换为 <Blank />
2. $x(n)=\cos(\frac{n\pi}{4})-3\sin(\frac{n\pi}{6})$的周期为<Blank />
3. 设$x(t)=\cos(11\pi t)$，以 $\Omega_s=15\pi \ \mathrm{rad/s}$ 对此信号进行离散化，然后使用截止频率为 $\Omega_{max}=8\pi \ \mathrm{rad/s}$ 的理想低通滤波器恢复得到模拟信号为<Blank />

## 二
一线性非移变系统的输入和输出满足差分方程：$y(n)=x(n)-0.5y(n-1)$
1. 求系统函数 $H(z)$
2. 写出所有可能的收敛域下系统的单位冲激响应 $h(n)$
3. 写出系统频率响应 $H(e^{j\omega})$

## 四
已知序列 $x(n)=n+3,\ 0\le n\le 9$，$h(n)=\{1,2,3\}$
按 $N=7$，对序列$x(n)$分段，并利用重叠保留法计算线性卷积 $y(n)=x(n)*h(n)$

## 五
对模拟信号：$x(t)=\cos(4000\pi t)$ 以 $T=0.25\ \mathrm{ms}$间隔采样
1. 写出采样后序列 $x(n)$ 表达式和对应数字频率
2. 求 $x(n)$ 的4点DFT $X(k)$
3. 序列 $x_1(n)=x(n)w(n)$，$w(n)=u(n)-u(n-4)$，画出 $x_1(n)$ 的图形
4. $x_1(n)$ 输入LTI离散系统 $h(n)=\{1,2,3\}$，采用线性时域卷积计算输出 $y(n)$

## 六、IIR滤波器
1. 设模拟系统函数 $H_a(s)=\frac{s+0.2}{(5s+0.2)^2+4}$，取样周期 $T=1$，请用冲激响应不变法将其转化为数字系统 $H(z)$
2. 已知模拟低通滤波器的归一化传输函数为 $ H_a(p)=\frac{1}{p+1}$，试用双线性变换法设计一个数字低通滤波器，其3dB截止频率 $\omega_c=0.5\pi \ \mathrm{rad/s}$，采样周期 $T=1\ \mathrm{s}$
    1. 求预畸变后的模拟截止频率
    2. 写出模拟低通滤波器的传输函数
    3. 写出所设计的数字滤波器系统函数
