---
科目: 通信原理II
时间: 2025-2026学年第二学期
阶段: 期末
类型: 本科
学院:
- 信息与通信工程学院
---

近似公式：
$\log_23\approx \frac85,\log_210\approx\frac{10}3$

## 一、选择

1.

2. 哈玛达矩阵的递推关系，正确的是：
<Choices>
	+ $H_{2N}=\begin{pmatrix}H_N&H_N\\H_N&-H_N\end{pmatrix}$
</Choices>

3. 

4. 扩频通信DS-BPSK受到另一扩频信号影响（多址干扰），扩频系数$N$，则解扩后信扰与<Slot/>成正比。
<Choices>
	- $\mathrm e^N$
	- $1nN$
	- $N$
	- $\sqrt{N}$
</Choices>

5. BSC 信道的容量，误码率等$\frac12$时为<Slot item="5"/>？误码率为$\frac34$时为<Slot item="6"/>？
<Choices item="5">
	- $0$
	- $\frac12$
	- $\frac14$
	- $\frac15$
</Choices>
<Choices item="6">
	- $0$
	- $\frac12$
	- $\frac14$
	- $\frac15$
</Choices>

6. 将突发差错变为随机差错用<Slot item="7"/>？将发送数据随机化用<Slot item="8"/>？
<Choices item="7">
	- 交织
	- 分集
	- 扰码
	- 
</Choices>
<Choices item="8">
	- 交织
	- 分集
	- 扰码
	- 
</Choices>

7. AWGN信道单边噪声功率谱密度$N_0=200\,\mathrm{mW/kHz}$，则信噪比为<Slot item="9"/>$\mathrm{dB}$，信道容量为$C=$<Slot item="10"/>$\mathrm{kbit}$。
<Choices item="9">
	- 10
	- 20
	- 30
	- 40
</Choices>
<Choices item="10">
	- 10
	- 20
	- 30
	- 40
</Choices>

8. 描述信道时间选择性的参数在时域体现为<Slot item="11"/>？在频域体现为<Slot item="12"/>？
<Choices item="11">
	- 多普勒频移
	- 时延扩展
	- 相干带宽
	- 相干时间
</Choices>
<Choices item="12">
	- 多普勒频移
	- 时延扩展
	- 相干带宽
	- 相干时间
</Choices>

9. 下面最适合OFDM的是？

10. OFDM 加循环前缀CP是为了？
<Choices>
	- 保持子载波正交性
	- 保持子载波在频谱上不交叠
	- 提高频谱利用率
	- 降低PAPR
</Choices>

11. CP时间通常大于？
<Choices>
	- 采样频率
	- 信号带宽
	- 最大时延扩展
	- 
</Choices>

## 二

$X$等概率取$\{\pm1,\pm3\}$，$Z$以$\frac14,\frac12,\frac14$概率分别取$\{-1,0,1\}$，$Y=X+Z$。

求$H(X),H(Z),Y$的概率分布及$H(Y)$，条件熵$H(Y\mid X)$，互信息$I(Y;X)$。

<Solution>
$H(x)=2,H(Z)=1.5,H(Y)=\frac{25}8,H(Y\mid X)=1.5,I(Y;X)=\frac{13}8$

单位均为$\mathrm{bit}$或$\mathrm{bit/symbol}$
</Solution>

## 三

$(8,4)$线性分组码。已知矩阵$G=$。
1. 通过初等变换将$G$化为系统码，并写出监督矩阵$H$。
2. 接收码字$(11100010)$，求伴随式译码结果。

## 四

已知$x^7+1=(x^3+x+1)(x^4+x^2+x+1)$，据此写出$(7,3)$循环码的生成多项式；系统码生成矩阵；所有码字；最小码距。

## 五

<Figure src="题图5.svg" float>
	第五题图
</Figure>

卷积码如图所示。

1. 请画出状态转移图；
2. 给出11100的编码输出码字；
3. 给出（？）的译码结果（方式不限，给出译码器输出的5个比特）
<Solution>
10100，错一位。
</Solution>

## 六

$m$序列。已知两个LSFR线性反馈移位寄存器的特征多项式$f_1(x)=(1+x^3),g_2(x)=(1+x^2+x^3)$。

输出序列分别是$\{a_k^{(1)}\},\{a_k^{(2)}\}$，设$\{a_{-1}^{(1)}\}=\{a_{-1}^{(2)}\}=1$, $\{a_{-2}^{(1)}\}=\{a_{-2}^{(2)}\}=\{a_{-3}^{(1)}\}=\{a_{-3}^{(2)}\}=0$

分别画出框图，写出它们的输出序列，写出周期，并指出是否为$m$序列。
